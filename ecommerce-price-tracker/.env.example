# Copy to .env and adjust if needed. .env is gitignored (local dev only).
DATABASE_URL=postgresql+psycopg2://pricetracker:pricetracker@localhost:5433/pricetracker
CORS_ORIGINS=http://localhost:5173
SCRAPE_INTERVAL_MINUTES=60
SCRAPE_ON_STARTUP=true
HEADLESS=true
